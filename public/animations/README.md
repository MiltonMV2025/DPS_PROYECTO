# Empty state animation

`empty-search.json` is a locally bundled animation provided for this project and used as the visual for table empty states.

Source: User-provided asset (`Search File.json`).

The animation is loaded locally so the application does not depend on a runtime CDN.
